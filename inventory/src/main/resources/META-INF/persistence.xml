<persistence
        version="2.1"
        xmlns="http://xmlns.jcp.org/xml/ns/persistence"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
                            http://xmlns.jcp.org/xml/ns/persistence_2_1.xsd">

    <!--
         The <code>persistence.xml</code> file configures at least one persistence unit;
         each unit must have a unique name.
    -->


    <persistence-unit name="ordersPersistenceUnit">

        <properties>

            <property name="javax.persistence.jdbc.driver" value="org.postgresql.Driver"/>
            <property name="javax.persistence.jdbc.url" value="jdbc:postgresql://localhost:5432/Orders"/>
            <property name="javax.persistence.jdbc.user" value="postgres"/>
            <property name="javax.persistence.jdbc.password" value="99ronore"/>
            <property name="hibernate.format_sql" value="true"/>
            <property name="javax.persistence.schema-generation.database.action" value="validate"/>
            <!-- hibernate specific config - SQL visible in console -->
            <property name="hibernate.show_sql" value="true"/>

            <!-- hibernate specific config - additional comments with SQLs in console -->
            <property name="hibernate.use_sql_comments" value="true"/>
            <property name="org.jboss.logging.provider" value="log4j2"/>

        </properties>
    </persistence-unit>
</persistence>